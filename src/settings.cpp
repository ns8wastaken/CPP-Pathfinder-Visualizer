#include "settings.hpp"


// Colors
const sf::Color Colors::Lines = sf::Color(150, 150, 150); // Gray

const sf::Color Colors::Empty       = sf::Color(255, 255, 255); // White
const sf::Color Colors::Wall        = sf::Color(  0,   0,   0); // Black
const sf::Color Colors::Start       = sf::Color(  0, 255,   0); // Green
const sf::Color Colors::End         = sf::Color(255,   0,   0); // Red
const sf::Color Colors::Checkpoint  = sf::Color(115,   0, 255); // Purple

const sf::Color Colors::Search  = sf::Color(110, 110, 110); // Dark gray
const sf::Color Colors::Visited = sf::Color(140, 140, 140); // Light gray
const sf::Color Colors::Path    = sf::Color(50,  112, 255); // Blue
// const sf::Color Colors::Path    = sf::Color(251, 133,   0); // Orange
// const sf::Color Colors::Path    = sf::Color(255, 183,   3); // Yellow
